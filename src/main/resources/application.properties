spring.application.name=tatsam-priority
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#if database is mysql then no need to change, if oracle change to  org.hibernate.dialect.OracleDialecta
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# here need change connection details of database host
spring.datasource.url=jdbc:mysql://localhost:3306/tatsam_priority?useSSL=false

# here need change connection details of database host username
spring.datasource.username=root

# here need change connection details of database host password
spring.datasource.password=Pavan.90

spring.jackson.serialization.fail-on-empty-beans=false